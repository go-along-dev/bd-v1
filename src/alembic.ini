# =============================================================================
# alembic.ini — Alembic Configuration
# =============================================================================
# See: system-design/11-db-schema-ddl.md §16 "Migration Checklist"
#
# TODO: Configure for async PostgreSQL:
#   [alembic]
#   script_location = alembic
#   sqlalchemy.url = postgresql+asyncpg://... (or use env var override in env.py)
#
# TODO: Set file_template for readable migration filenames:
#   file_template = %%(year)d_%%(month).2d_%%(day).2d_%%(rev)s_%%(slug)s
#
# TODO: In env.py, override sqlalchemy.url with config.SUPABASE_DB_URL at runtime.
#       Never hardcode credentials in this file.
#
# Commands:
#   alembic revision --autogenerate -m "description"
#   alembic upgrade head
#   alembic downgrade -1
#   alembic history
#
# work by adolf.
